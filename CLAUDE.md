# Project Guidelines

## Git Push Policy
- Before every `git push`, ALWAYS ask the user to confirm their git identity (name and email) first.
- Never push without explicit user confirmation of identity, regardless of device or context.
